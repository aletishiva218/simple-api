<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap');
*{
    margin: 0;
    padding: 0;
    font-family: 'Poppins', sans-serif;
}

body{
    background-color: #30336b;
    color: #30336b;
    font-size: 20px;
}

.container{
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%,-50%);
    display: flex;
    flex-direction: column;
    row-gap: 1rem;
    background-color: white;
    padding: 0.5rem 2rem;
}

form{
    display: flex;
    flex-direction: column;
    row-gap: 1rem;
}

form label{
    display: flex;
    flex-direction: column;
    row-gap: 0.5rem;
}

form label input{
    border: 1px solid #30336b;
    padding: 0.5rem;
    width: auto;
    min-width: 20rem;
}

form button,form input[type="submit"]{
    background-color: #30336b;
    color: white;
    border: none;
    padding: 0.5rem;
    font-size: 20px;
}

.container > span:last-child{
    margin-bottom:1rem;
}

h1{
    text-align: center;
}
form .error{
    color: red;
}
    </style>
</head>
<body>
    <div class="container">
        <h1>Login</h1>
        <form action="/user/login" method="POST">
            <label for="username"><span>username</span><input type="text" name="username" id="username" required></label>
            <label for="password"><span>password</span><input type="password" name="password" id="password" required></label>
            <% if(locals.notexists) {%>
                <span class="error">Invalid username or password</span>
                <%}%>
            <button type="submit">Login</button>
        </form>
        <span>forgot password ? <a href="/user/forgotpassword">reset</a></span>
        <span>Dont have an account ? <a href="/user/register">SignUp</a></span>
    </div>
</body>
</html>