<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap');
*{
    margin: 0;
    padding: 0;
    font-family: 'Poppins', sans-serif;
}

body{
    background-color: #30336b;
    color: #30336b;
    font-size: 20px;
}

.container{
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%,-50%);
    display: flex;
    flex-direction: column;
    row-gap: 1rem;
    background-color: white;
    padding: 0.5rem 2rem;
}

form{
    display: flex;
    flex-direction: column;
    row-gap: 1rem;
}

form label{
    display: flex;
    flex-direction: column;
    row-gap: 0.5rem;
}

form label input{
    border: 1px solid #30336b;
    padding: 0.5rem;
    width: auto;
    min-width: 20rem;
}

form button,form input[type="submit"]{
    background-color: #30336b;
    color: white;
    border: none;
    padding: 0.5rem;
    font-size: 20px;
}

.container > span:last-child{
    margin-bottom:1rem;
}

h1{
    text-align: center;
}
form .error{
    color: red;
}
    </style>
</head>
<body>
    <div class="container">
        <% if(!locals.isChange) {%>
        <h1>Forgot password ?</h1>
        <%} else {%>
        <h1>Change Password</h1>
        <%}%>
        <form method="POST">
            <% if(!locals.isChange) {%>
                <label for="username"><span>username</span><input type="text" name="username" id="username" required></label>
                <%} else {%>
                    <label for="username" style="display:none"><span>username</span><input type="text" name="username" id="username" value="<%= username %>"></label>
                 <label for="password1"><span>new password</span><input type="password" name="password1" id="password1" required></label>
            <label for="password2"><span>confirm password</span><input type="password" name="password2" id="password2" required></label>
                <%}%>
                <% if(locals.notexists) {%>
                    <span class="error">Invalid username</span>
                    <%}%>
                    <% if(locals.passwordNotMatch) {%>
                        <span class="error">password does not match</span>
                        <%}%>
            <% if(!locals.isChange) {%>
                <input type="submit" value="Request to Reset" formaction="/user/forgotpassword">
                <%} else {%>
                   <input type="submit" value="Change" formaction="/user/changepassword">         
                   <%}%>
        </form>
        <span><a href="/user/login">Back to login</a></span>
    </div>
</body>
</html>